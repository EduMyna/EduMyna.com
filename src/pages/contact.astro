---
import Layout from "@/layouts/Layout.astro";
import Container from "@/components/container.astro";
import Contactform from "@/components/contactform.astro";
import { Icon } from "astro-icon/components";

const success = Astro.url.searchParams.get("success") === "1";
const GITHUB_REPO = "https://github.com/EduMyna/EduMyna";
const EMAIL = "gauthamail@gmail.com";
---

<Layout
  title="Contact"
  description="Talk to a human. Share your context. We will respond with next steps."
>
  <!-- Hero band -->
  <section class="relative overflow-hidden bg-slate-50 border-b border-slate-200">
    <div
      class="pointer-events-none absolute inset-0 bg-gradient-to-b from-white via-slate-50 to-slate-100"
    />
    <Container>
      <div class="relative max-w-4xl py-20">
        <h1 class="text-5xl lg:text-6xl font-bold tracking-tight">
          Contact
        </h1>
        <p class="mt-6 text-xl text-slate-600 max-w-3xl">
          If you’re running a real learning program and want a platform that supports structure and feedback loops,
          share your context. We’ll respond with next steps.
        </p>

        {
          success && (
            <div class="mt-8 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-emerald-900">
              <div class="flex items-start gap-3">
                <div class="mt-0.5">
                  <Icon class="w-5 h-5" name="bx:bxs-check-circle" />
                </div>
                <div>
                  <p class="font-semibold">Message received.</p>
                  <p class="text-sm mt-1 text-emerald-800">
                    We’ll get back to you soon.
                  </p>
                </div>
              </div>
            </div>
          )
        }
      </div>
    </Container>
  </section>

  <Container>
    <section class="max-w-6xl mx-auto py-20">
      <div class="grid lg:grid-cols-[1fr_1.2fr] gap-10 items-start">
        <!-- Left: contact cards -->
        <div class="space-y-6">
          <div class="rounded-2xl border border-slate-200 bg-white p-6">
            <h2 class="text-2xl font-semibold">Quick ways to reach us</h2>
            <p class="mt-2 text-slate-600">
              Use the form if you want a structured reply. Use email if you want speed.
            </p>

            <div class="mt-6 space-y-4">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center">
                  <Icon class="w-5 h-5 text-slate-700" name="uil:envelope" />
                </div>
                <div>
                  <div class="text-sm text-slate-500">Email</div>
                  <a class="font-semibold hover:underline" href={`mailto:${EMAIL}`}>{EMAIL}</a>
                </div>
              </div>

              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center">
                  <Icon class="w-5 h-5 text-slate-700" name="bx:bxl-github" />
                </div>
                <div>
                  <div class="text-sm text-slate-500">Open-source repo</div>
                  <a
                    class="font-semibold hover:underline"
                    href={GITHUB_REPO}
                    target="_blank"
                    rel="noopener noreferrer"
                  >
                    github.com/EduMyna/EduMyna
                  </a>
                </div>
              </div>

              <div class="flex items-center gap-3">
                <div class="w-10 h-10 rounded-xl bg-slate-100 flex items-center justify-center">
                  <Icon class="w-5 h-5 text-slate-700" name="uil:map-marker" />
                </div>
                <div>
                  <div class="text-sm text-slate-500">Location</div>
                  <div class="font-semibold">India (remote-friendly)</div>
                </div>
              </div>
            </div>
          </div>

          <div class="rounded-2xl border border-slate-200 bg-slate-50 p-6">
            <h3 class="text-lg font-semibold">What to include (so we can answer fast)</h3>
            <ul class="mt-3 list-disc pl-5 text-slate-600 space-y-2">
              <li>What you are running: cohort, bootcamp, internal training, or something else</li>
              <li>Number of learners per cohort and frequency</li>
              <li>What success looks like for you</li>
              <li>Whether you want self-hosted or managed hosting</li>
            </ul>
          </div>
        </div>

        <!-- Right: form -->
        <div class="rounded-2xl border border-slate-200 bg-white p-8">
          <h2 class="text-2xl font-semibold">Send a message</h2>
          <p class="mt-2 text-slate-600">
            We’ll respond with next steps. If it’s not a fit, we’ll still be direct.
          </p>

          <div class="mt-8">
            <Contactform />
          </div>
        </div>
      </div>
    </section>
  </Container>
</Layout>